# Labels

A **minimal, useful** label taxonomy — enough to slice work meaningfully (e.g. a Problem
issue as bug / incident / vulnerability) without creating admin burden.

Teams may **add** labels additively but must not diverge from shared ones.

!!! info "Forthcoming"
    The canonical label taxonomy for issues and projects is defined in **GRI-73**.
